def fill_random_items_with_key:
    for room in all_rooms:
        items_of_room = object_relations[room]
        get_random_number(0, len(items_of_room) -1)
        furniture = items_of_room[2]
        door = for......
        key = .......
        object_relations Add couch:key_a
